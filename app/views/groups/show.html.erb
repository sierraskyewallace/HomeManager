


<h5> Your Group | <%= @group.name %></h5>
<%= link_to "Edit", edit_group_path(@group) %> |
<%= link_to "Delete", @group, method: :delete, data: { confirm: "Are you sure?" } %><br>

            <div class="card-body">
              <div class="invite-box mb-4" data-controller="invite">
                <h5>Invite Link:</h5>
                <input class="group-url" id="cb-link-field" type="text" value="<%= new_group_group_membership_url(@group) %>" data-target="invite.url" data-group-token="<%= @group.invite_token %>"/>

                </div>
              </div>

             <ul> <% @group.group_memberships.each do |g| %>
              <li>  <%= g.user.email %>
                <% end %></li>
                </ul>

<%= link_to 'Group Tasks', group_tasks_path(@group) %>

        