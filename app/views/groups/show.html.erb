<h1> group show page </h1>

<%= @group.name %> <br>
<% @group.users.each do |u| %>
    <% u.email %>
    <% end %>

<p> add remove user feature next to each user </p>

<%= link_to 'Invite User', new_group_invite_path(@group, invite: { invitable_id: @group.id, invitable_type: 'Group' } ) %>
