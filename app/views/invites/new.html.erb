<%= form_for [@group, @invite], url: group_invites_path do |f| %>
    <%= f.hidden_field :group_id, :value => @invite.group_id %>
    <%= f.label :email %>
    <%= f.email_field :email %>
    <%= f.submit 'Send' %>
<% end %>