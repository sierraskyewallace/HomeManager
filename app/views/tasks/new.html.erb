<%= form_for([@list, Task.new]) do |f| %>
    <%= f.text_field :name, placeholder: "Create a task" %>
    <%= f.collection_select :member_id, @members, :id, :name, {}, :prompt => "Assign a task" %>
    <%= f.collection_select :list, List.all, :id, :name %>
    <%= f. submit %>
  <% end %>